import React from 'react';
import Fade from 'react-reveal/Fade';

const contact = () => {
  return (
    <Fade bottom>
      <section className="contact-box section-padding">
        <h4>Want to chat?</h4>
        <p>I'm currently looking</p>
      </section>
    </Fade>
  );
};

export default contact;
